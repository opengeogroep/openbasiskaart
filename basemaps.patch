diff --git a/Makefile b/Makefile
index 2a4eb7c..3db7c1d 100644
--- a/Makefile
+++ b/Makefile
@@ -12,14 +12,11 @@ CPP=gcc -E -x c
 
 OSM_PREFIX=osm_new_
 OSM_NAME_COLUMN=name
-OSM_SRID=4326
-OSM_UNITS=dd
-OSM_EXTENT=-180 -90 180 90
-OSM_SRID=3857
-OSM_UNITS=meters
 OSM_DB_CONNECTION=host=localhost dbname=osm user=osm password=osm port=5432
-OSM_EXTENT=-20000000 -20000000 20000000 20000000
-OSM_WMS_SRS=EPSG:900913 EPSG:4326 EPSG:3857 EPSG:900913 EPSG:2154 EPSG:310642901 EPSG:4171 EPSG:310024802 EPSG:310915814 EPSG:310486805 EPSG:310702807 EPSG:310700806 EPSG:310547809 EPSG:310706808 EPSG:310642810 EPSG:310642801 EPSG:310642812 EPSG:310032811 EPSG:310642813 EPSG:2986
+OSM_SRID=28992
+OSM_UNITS=meters
+OSM_EXTENT=12000 304000 280000 620000
+OSM_WMS_SRS=EPSG:4326 EPSG:900913 EPSG:28992 EPSG:3857
 DEBUG=1
 LAYERDEBUG=1
 STYLE=default
@@ -38,7 +35,7 @@ includes=land.map landusage.map borders.map highways.map places.map \
 
 
 mapfile=osm-$(STYLE).map
-here=`pwd`
+here=/usr/share/proj
 
 all:$(mapfile) boundaries.sql
 
@@ -113,6 +110,8 @@ $(mapfile):$(template) $(includes)
 	$(SED) -e 's/OSM_WMS_SRS/$(OSM_WMS_SRS)/g' $(mapfile)
 	$(SED) -e 's/OSM_NAME_COLUMN/$(OSM_NAME_COLUMN)/g' $(mapfile)
 	$(SED) -e 's/OSM_DB_CONNECTION/$(OSM_DB_CONNECTION)/g' $(mapfile)
+	$(SED) -e 's/OSM_EXTENT/$(OSM_EXTENT)/g' $(mapfile)
+
 
 boundaries.sql: boundaries.sql.in
 	cp -f $< $@
diff --git a/buildings.map b/buildings.map
index bb23ff5..1006b49 100644
--- a/buildings.map
+++ b/buildings.map
@@ -6,6 +6,9 @@ LAYER
     END
     GROUP "default"
     NAME layername(buildings,_layer_suffix)
+    METADATA
+      "wms_extent" "OSM_EXTENT"
+    END
     TYPE POLYGON
     CONNECTIONTYPE postgis
     CONNECTION "OSM_DB_CONNECTION"
diff --git a/generate_style.py b/generate_style.py
index 5666d07..e2798fc 100755
--- a/generate_style.py
+++ b/generate_style.py
@@ -981,6 +981,9 @@ vars= {
 
 styles = {
    'default': {},
+   'nb': {
+     'display_buildings':0 
+   },
    'outlined':{
       'display_motorway_outline': {
          0:0,
@@ -1287,6 +1290,7 @@ style_aliases = {
    # map with no road casing and few colors, suited for using as a basemap when overlaying
    # other layers without risk of confusion between layers.
    "default":"default",
+   "nb":"nb",
 
    # a style resembling the google-maps theme
    "google":"default,outlined,google",
diff --git a/highways.map b/highways.map
index 9776ba9..20c6cc8 100644
--- a/highways.map
+++ b/highways.map
@@ -8,6 +8,9 @@ LAYER
     MAXSCALEDENOM _maxscale
     TYPE LINE
     NAME layername(railways,_layer_suffix)
+    METADATA
+      "wms_extent" "OSM_EXTENT"
+    END
     DATA _railways_data
     CLASSITEM "tunnel"
     CLASS
@@ -42,6 +45,9 @@ LAYER
     MINSCALEDENOM _minscale
     MAXSCALEDENOM _maxscale
     NAME layername(roads,_layer_suffix)
+    METADATA
+      "wms_extent" "OSM_EXTENT"
+    END
     CONNECTIONTYPE postgis
     CONNECTION "OSM_DB_CONNECTION"
     DATA _roads_data
@@ -867,6 +873,9 @@ LAYER
     MAXSCALEDENOM _maxscale
     TYPE LINE
     NAME layername(aeroways,_layer_suffix)
+    METADATA
+      "wms_extent" "OSM_EXTENT"
+    END
     CONNECTIONTYPE postgis
     CONNECTION "OSM_DB_CONNECTION"
     DATA "geometry from (select geometry, osm_id, type from OSM_PREFIX_aeroways) as foo using unique osm_id using srid=OSM_SRID"
diff --git a/land.map b/land.map
index d631b9c..dea78c4 100644
--- a/land.map
+++ b/land.map
@@ -4,6 +4,9 @@ LAYER
    GROUP "default"
    DEBUG _layerdebug
    NAME layername(land,_layer_suffix)
+   METADATA
+     "wms_extent" "OSM_EXTENT"
+   END
    PROCESSING "APPROXIMATION_SCALE=full"
    PROJECTION
        _land_epsg
diff --git a/landusage.map b/landusage.map
index 858ca46..d6687fb 100644
--- a/landusage.map
+++ b/landusage.map
@@ -6,6 +6,9 @@ LAYER
         "init=epsg:OSM_SRID"
     END
     NAME layername(landuse,_layer_suffix)
+    METADATA
+      "wms_extent" "OSM_EXTENT"
+    END
     GROUP "default"
     CONNECTIONTYPE POSTGIS
     CONNECTION "OSM_DB_CONNECTION"
@@ -233,6 +236,9 @@ LAYER
        "init=epsg:OSM_SRID"
    END
    NAME layername(transport_areas,_layer_suffix)
+   METADATA
+     "wms_extent" "OSM_EXTENT"
+   END
    GROUP "default"
    CONNECTIONTYPE POSTGIS
    CONNECTION "OSM_DB_CONNECTION"
@@ -273,6 +279,9 @@ LAYER
         "init=epsg:OSM_SRID"
     END
     NAME layername(waterarea,_layer_suffix)
+    METADATA
+      "wms_extent" "OSM_EXTENT"
+    END
     GROUP "default"
     CONNECTIONTYPE POSTGIS
     CONNECTION "OSM_DB_CONNECTION"
@@ -320,6 +329,9 @@ END
 LAYER
     TYPE LINE
     NAME layername(waterways,_layer_suffix)
+    METADATA
+      "wms_extent" "OSM_EXTENT"
+    END
     STATUS ON
     GROUP "default"
     CONNECTIONTYPE POSTGIS
diff --git a/places.map b/places.map
index 3b5ce35..2fda4f1 100644
--- a/places.map
+++ b/places.map
@@ -6,6 +6,9 @@ LAYER
  CONNECTIONTYPE postgis
  CONNECTION "OSM_DB_CONNECTION"
  NAME layername(places,_layer_suffix)
+ METADATA
+   "wms_extent" "OSM_EXTENT"
+ END
  MINSCALEDENOM _minscale
  MAXSCALEDENOM _maxscale
  DATA _places_data
